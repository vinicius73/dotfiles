# Copy this file to ~/.config/dotfiles/secrets.conf and keep the copy mode 0600.
# Add one environment-variable name per line. Names declared here may be loaded
# from env.d and, on macOS, are looked up in Keychain by service name and $USER.
# Do not commit your local secrets.conf or env.d files.
EXAMPLE_TOKEN
